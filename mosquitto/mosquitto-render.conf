# Config Render — WebSocket uniquement (Render n'expose que HTTP/HTTPS)
# Le port 10000 est imposé par Render ($PORT par défaut)
listener 10000
protocol websockets
allow_anonymous true
persistence false
log_dest stdout
